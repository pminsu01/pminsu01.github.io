---

title: Ubuntu 15.10 install
category : Setting
layout : post
published : true

---
ubuntu 15.10 설치를 위하여 노트북에서 설정해야 할 것은

```
acpi=off or noacpi
```

add option


Ubuntu 첫 설치 뒤에 처리해야할 명령어

```
$sudo apt-get update
$sudo apt-get upgrade
$sudo apt-get autoremove
$sudo apt-gee autoclean

```


System Settings -> software & upgrade


### 설치해야할 프로그램 목록

```
- dropbox
- chrome
- atom

```

##Jeklly 블로그 설치

```
gem install jekyll

```


### 한국어 입력하기

```
sudo apt-get install uim uim-byeoru

```

System_Setting  => Text Entry

korean(101/104 key compatible) 추가하기
Shortcuts : key to show the HUD가 설정되어 있는키 -> 다른 키로 바꾸기  (한글키로 설정되어 있기 때문에)

검색 => input method

global settings

specify default im -> 체크
default input method  -> Byeoru
Enable input method -> Byeoru

Byeoru key bindings 1 가서

[Byeoru] on , off  => 한/영 키로 바꾸기.



### 명령어 위치 변경하기

```
sudo ln -s /usr/local/bin/jekyll user/bin/jekyll
              A                     B
```
B의 링크를 A링크로 명령어 위치 변경하기
